# KSL LAB — Evolution Manifest

KSL is evolving as a living signal layer.

Validated layers now present:

- canonical mapping
- persistent trace
- timeline append
- dual rendering path: ASCII and ANSI
- event priority layer
- runtime hook layer
- session binding scaffold

Historical divergence remains visible:

- runtime emitted router.fallback_local before mapping coverage existed
- the system stayed stable
- trace preserved the break
- canon was updated without deleting evidence

Current direction:

- KSL is no longer only a renderer
- KSL is becoming a runtime-bound signal fabric
- session events can now be routed into the signal layer through dedicated hooks
